// Browser app site configuration.
//
// Loaded before app.js. The default below is for serving the repository
// locally, where data/attachments/ sits alongside web/ and attachment links can
// resolve to those files directly.
//
// The GitHub Pages deploy workflow overwrites this file with an
// attachmentBaseUrl pointing at the attachments mirror repository, since
// attachment bytes are not published to Pages.
window.DISCORD_HISTORY_CONFIG = {};
